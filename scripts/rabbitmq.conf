# Límite de memoria de RabbitMQ: 512 MiB.
# RabbitMQ deja de aceptar mensajes (pausa productores) al llegar a este tope,
# evitando alarmas de system_memory_high_watermark y OOM con el mem_limit de Docker.
vm_memory_high_watermark.absolute = 512MiB
